# Highway Alignment Generator – Python Dependencies
# Install with:  pip install -r requirements.txt

geopandas>=0.14
rasterio>=1.3
osmnx>=1.9
shapely>=2.0
scipy>=1.11
scikit-image>=0.22
pyproj>=3.6
numpy>=1.26
requests>=2.31

# ── Optional Accelerators (Install when possible) ────────

# Fast Marching Method path engine — smoother paths on continuous terrain (Phase 4)
# pip install scikit-fmm

# JIT acceleration for cost surface — 10-50x speedup (Phase 5)
# pip install numba>=0.57

# ── Phase 5.3: Visualization Suite ────────────────────────

# Interactive HTML map generation
folium>=0.15
matplotlib>=3.8
python-dotenv>=1.0

# ── Phase 10: Feasibility Report ────────────────────────
# HTML/PDF report generation
jinja2>=3.1
weasyprint>=60.0

# ── Phase 11: Enhanced Data Sources ─────────────────────
# ESA WorldCover 10m land cover (replaces incomplete OSM LULC)
pystac-client>=0.7
rioxarray>=0.15
planetary-computer>=1.0

# Overture Maps building footprints (ML-derived, better rural coverage)
overturemaps>=0.5
