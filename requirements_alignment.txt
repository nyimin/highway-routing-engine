# Highway Alignment Generator – Python Dependencies
# Install with:  pip install -r requirements.txt

geopandas>=0.14
rasterio>=1.3
osmnx>=1.9
shapely>=2.0
scipy>=1.11
scikit-image>=0.22
pyproj>=3.6
numpy>=1.26
requests>=2.31

# ── Optional Accelerators (Install when possible) ────────

# Fast Marching Method path engine — smoother paths on continuous terrain (Phase 4)
# pip install scikit-fmm

# JIT acceleration for cost surface — 10-50x speedup (Phase 5)
# pip install numba>=0.57

# ── Phase 5.3: Visualization Suite ────────────────────────

# Interactive HTML map generation
folium>=0.15
matplotlib>=3.8
python-dotenv>=1.0
